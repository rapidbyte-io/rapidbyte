//! Shared test utilities for the API crate.
//!
//! Provides `test_api_context()` to build an `ApiContext` backed by
//! in-memory fakes. Completed in Task 11 when all services are wired.

// Note: This module is gated by #[cfg(test)] in lib.rs.
// The test_api_context() factory will be completed in Task 11
// after all 6 service implementations exist.
