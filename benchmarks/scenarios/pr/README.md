# PR Benchmark Suite

This directory holds the small, stable benchmark scenarios intended for pull
request regression detection.

The PR suite should remain synthetic-first, low-noise, and fast enough to run as
part of normal CI.
