# Legacy Benchmark Fixtures

These files were migrated out of `tests/bench/` during the benchmark platform
rewrite.

They are preserved as reference inputs while the new scenario-driven benchmark
system grows equivalent workload coverage.
